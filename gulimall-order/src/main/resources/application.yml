server:
  port: 9000
spring:
  cloud:
    nacos:
      discovery:
        server-addr: localhost:5000

mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto